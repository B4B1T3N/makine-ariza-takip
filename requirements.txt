# Makine Arıza Takip Sistemi - çalışma zamanı bağımlılıkları (bulut sürümü)
# Kurulum:  pip install -r requirements.txt

# --- Veritabanı ---
psycopg[binary]>=3.1,<4    # PostgreSQL sürücüsü
psycopg-pool>=3.2,<4       # Bağlantı havuzu
tzdata>=2024.1             # Windows'ta zoneinfo için IANA saat dilimi verisi

# --- Web arayüzü (Faz 2) ---
fastapi>=0.110,<1          # HTTP yönlendirme ve doğrulama
uvicorn[standard]>=0.29,<1 # ASGI sunucusu
jinja2>=3.1,<4             # Sunucu taraflı şablonlar
itsdangerous>=2.1,<3       # Oturum çerezinin imzalanması
python-multipart>=0.0.9    # HTML form gövdelerinin çözümlenmesi

# --- Nesne depolama (Faz 4) ---
# Yalnızca ekler S3 uyumlu bir servise taşınacaksa (MAT_STORAGE=s3) gerekir.
# Varsayılan yerel diskte hiçbir ek paket istemez, bu yüzden kapalı bırakıldı:
# boto3>=1.34,<2

# --- Raporlama ---
openpyxl>=3.1,<4           # Excel (.xlsx) dışa aktarma

# --- Masaüstü arayüz ---
# Faz 1'de masaüstü arayüz PostgreSQL'e karşı çalışmaya devam ediyor; geçişin
# doğruluğunu kanıtlayan referans budur. Faz 3 sonunda web arayüzü devralınca
# bu iki satır kaldırılabilir.
PyQt6>=6.6,<7
matplotlib>=3.8,<4
